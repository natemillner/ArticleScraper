from articles import tc,intel
from utils import purge_duplicate_articles, add_articles,update_date

# articles = tc.read_articles(purge_duplicate_articles(tc.get_articles()))
# add_articles(articles)
# articles = intel.read_articles(purge_duplicate_articles(intel.get_articles()))
# add_articles(articles)
# epilogue
